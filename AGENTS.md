# 개발 원칙

## 범위와 변경

- 현재 요청에서 승인한 작업만 수행한다.
- 먼저 기존 코드와 공개 API를 읽고, 재사용할 부분과 실제로 없는 기능을 구분한다.
- 기존 저장소 구조, 함수명, 객체 형식, 패키지 관례를 유지한다.
- 최소 변경은 최소 줄 수가 아니라, 요구사항을 정확히 충족하는 작은 변경을 의미한다.
- 무관한 리팩터링, 전체 파일 재포맷, 스타일 목적의 이름 변경을 하지 않는다.
- 수정/추가할 파일과 dependency를 먼저 명시한다.
- 승인 범위를 넘어야 하면 이유를 보고하고, 범위를 확대하지 않은 상태에서 중단한다.
- 구현은 한 편집 주체가 담당한다. 병렬 subagent에게 파일 편집을 위임하지 않는다.
- 기존 사용자 변경을 보존한다. 요청 없이 commit, push, reset, 삭제를 하지 않는다.

## 기존 도구 재사용

- OHDSI의 데이터 추출, 정규화 회귀 기준 모델, matching, diagnostics를 재사용한다.
- 분산 실행에는 선정된 기존 도구 하나의 공개 API를 사용한다.
- 기존 패키지의 내부 함수나 존재하지 않는 확장 API를 있다고 가정하지 않는다.
- 외부 패키지를 사용했다는 이유만으로 OHDSI 또는 FL 호환성을 주장하지 않는다.
  실제 입출력과 실행 경로를 테스트한다.
- 통신·인증·직렬화·원격 실행을 제공하는 라이브러리 기능을 다시 구현하지 않는다.
- 새 라이브러리의 기본 estimator에 맞추려고 승인된 Bayesian LASSO/CCD를 바꾸지 않는다.
- 여러 backend를 지원하는 registry, factory, plugin architecture는 만들지 않는다.
- 외부 소스의 복사·수정이 필요하면 license, attribution, 유지보수 범위를 확인한다.

## R/C++ 코드 스타일

- Metis/extras/TestCode.r처럼 설정 → 데이터 준비 → 적합 → matching → 진단의 흐름을
  코드에서 직접 읽을 수 있게 작성한다.
- 예제의 오래된 API, 하드코딩, 불필요한 부분까지 복제하지 않는다.
- 명확한 이름과 직접적인 함수 호출을 우선한다.
- helper는 실제 계산 경계, 의미 있는 중복 제거, 수치 검증에 필요한 경우에만 추가한다.
- 짧은 코드를 옮기기 위해 utils.R, helpers.R 또는 새 모듈을 만들지 않는다.
- 함수 하나로 과도하게 합치거나 one-liner로 압축해 줄 수를 줄이지 않는다.
- C++에서는 기존 sparse 자료구조, 객체 수명, 메모리 소유권, cache 갱신 흐름을 유지한다.
- 기존 framework의 공개 확장점에서 요구하는 subclass는 허용하지만,
  자체적인 manager/class hierarchy를 만들지 않는다.
- 주석은 수식, 불변조건, 비직관적인 결정의 이유를 설명한다.

## 생성하지 않을 것

- 요청하지 않은 HE 구현, 암호화 placeholder, 대체 optimizer, RLPS/autoencoder.
- REST 서비스, 대시보드, 범용 FL 플랫폼, 새 설정 프레임워크.
- PLAN.md, TODO.md, STATUS.md, 중복 설계 문서, 중복 예제, 임의의 demo dataset.
- 요구되지 않은 DBMS·운영체제·패키지 버전 호환 계층.
- 실제 실험에 쓰지 않는 기능이나 미래 사용 사례를 위한 코드.

## 반드시 유지할 것

- 공개 입력/프로세스 경계에서 필요한 검증과 변경된 동작의 focused test.
- row/feature 정합성, binary label, 비유한 수치, 수렴 실패, 상태 불일치 검사.
- 명확한 오류 보고. 자동 데이터 삭제, estimator 교체, tolerance 완화로 실패를 숨기지 않는다.
- 필요한 DESCRIPTION/NAMESPACE 변경, native registration, export, 공개 함수 문서, 테스트.
- 기존 생성 도구가 있는 파일은 해당 도구로 갱신한다.
- 테스트용 작은 synthetic fixture는 코드로 생성하고 별도 데이터 artifact를 늘리지 않는다.

## 데이터와 실행

- 원본 CDM schema는 읽기 전용으로 취급한다.
- 승인된 analysis schema와 output directory에만 결과를 기록한다.
- 환자 행, 식별자, 자격증명, 환자별 PS, 실제 데이터의 중간 통계량을
  채팅·외부 서비스·소스 저장소에 노출하지 않는다.
- 동작에 필요한 모델·통계량 교환은 승인된 실행 환경과 프로토콜 안에서만 수행한다.
- 기존 환경변수와 연결 설정을 사용하고 secret 값을 출력하지 않는다.
- 승인 없이 전체 DB 실험이나 장시간 작업을 실행하지 않는다.
- synthetic test, 같은 프로세스의 simulation, 분리 worker 실행, 실제 DB 시험을 구분한다.
- 실행하지 않은 테스트나 결과를 성공했다고 보고하지 않는다.

## 완료 보고

- 변경 파일과 이유.
- 실제 실행한 명령과 결과.
- 미해결 오류, 미실행 시험, 성능 제약.
- 새 파일·dependency·public API 및 각각의 필요성.
- git diff 요약. 자동으로 다음 단계에 진입하지 않는다.
