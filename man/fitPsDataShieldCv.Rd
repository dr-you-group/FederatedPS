% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FederatedPs.R
\name{fitPsDataShieldCv}
\alias{fitPsDataShieldCv}
\title{Training-only preprocessing and fixed-grid global CV through DataSHIELD}
\usage{
fitPsDataShieldCv(
  connections,
  inputSymbol,
  runId,
  priorVariances = c(0.1, 1),
  folds = 2L,
  minFraction = 0.05,
  seed = 20260907L,
  maxFitSeconds = 300
)
}
\arguments{
\item{connections}{Two named, active DSI/DSOpal connections, one per site.}

\item{inputSymbol}{Name of an existing approved site-local input on each server.}

\item{runId}{A new alphanumeric run identifier, starting with a letter.}

\item{priorVariances}{Positive fixed grid, visited in increasing order.}

\item{folds}{Number of site-local treatment-stratified folds.}

\item{minFraction}{Training nonzero frequency threshold, inclusive.}

\item{seed}{Common deterministic seed; rows are sorted locally and comparator
then target groups are permuted separately. Fold assignments stay local.}

\item{maxFitSeconds}{R elapsed-time limit for each fixed-prior fit.}
}
\value{
Plain preprocessing summaries, candidate scores, selected variance,
fixed-prior fit diagnostics and final fit. No individual data or native
state is returned. Matching is a separate site-local operation on SUCCESS.
}
\description{
Training-only preprocessing and fixed-grid global CV through DataSHIELD
}
\details{
Uses existing \code{fitPsDataShield(..., returnPartial = FALSE)} for every
candidate/fold and the final full-population refit. Each fit starts at zero
in a new native state; transformed fold inputs are reused. All fits must
succeed. No candidate is omitted after a failure. Validation scores are
summed unpenalized logistic loss divided by the total validation count.
Scores within \code{1e-10} of the minimum select the smaller variance.

Input is a worker-local synthetic input with candidate references including
\code{timeWindow}, \code{valueType} (binary/count/continuous) and \code{isCollected = TRUE}.
Values must be finite and nonnegative; missing records mean zero. Supplied
control settings are preserved. Filtering/scaling are estimated anew from
training rows in each fold, then from all rows for the final refit. This is
not Cyclops' default internal CV or full FeatureExtraction tidying.
}
