% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PreparePsData.R
\name{preparePsData}
\alias{preparePsData}
\title{Prepare fixed propensity score inputs}
\usage{
preparePsData(sites, covariateIds, scales)
}
\arguments{
\item{sites}{Named list of exactly two sites. Each contains data frames
\code{population}, \code{covariates}, \code{covariateRef}, and \code{analysisRef}.
Population requires unique \code{rowId}, binary \code{treatment}, and \code{personSeqId}.
Covariates requires unique \verb{(rowId, covariateId)} pairs and finite
\code{covariateValue}. Identifiers are exact integer-valued R numeric vectors.}

\item{covariateIds}{Explicit common feature IDs in training order.}

\item{scales}{Positive divisors named in exactly the same feature order.}
}
\value{
A list with prepared \code{sites}, retained \code{covariateIds}, \code{excludedIds},
\code{scales}, and an explicit pooled \code{rowMap}. Each site has its original
population, one sparse \code{x}, named treatment \code{y}, and \code{cohortMethodData}.
The caller must use \code{Andromeda::close()} on each site's \code{cohortMethodData}
when finished.
}
\description{
Prepare fixed propensity score inputs
}
\details{
This boundary accepts extracted inputs; it does not query a CDM. Feature meanings
must agree across sites. Only globally zero features are excluded; a feature
absent from one site is retained. Scaling is supplied, not estimated.

An Andromeda-backed \code{CohortMethodData} is constructed using the table and class
convention in CohortMethod 6.0.3's \code{simulateCohortMethodData()}. Its outcomes
table has zero rows and typed \code{rowId}, \code{outcomeId}, and \code{daysToEvent} columns.
Matching/balance use the original unscaled covariates and population, not
only selected model coefficients. No clinical outcome is generated.
}
